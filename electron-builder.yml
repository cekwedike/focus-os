# electron-builder config — primary settings live in package.json "build".
# Run `pnpm generate:installer` before packaging to refresh NSIS artwork.

appId: com.focusos.app
productName: Focus OS

directories:
  output: release

win:
  icon: resources/icon.png
  signAndEditExecutable: false
  target:
    - target: nsis
      arch:
        - x64

nsis:
  include: resources/installer/installer.nsh
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: resources/icon.ico
  uninstallerIcon: resources/icon.ico
  installerHeader: resources/installer/header.bmp
  installerSidebar: resources/installer/sidebar.bmp
  shortcutName: Focus OS
  artifactName: ${productName} Setup ${version}.${ext}
