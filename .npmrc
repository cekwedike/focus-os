# pnpm uses a content-addressable store with symlinks into node_modules.
# Electron and better-sqlite3 resolve native .node binaries from predictable
# node_modules paths. Hoisted layout matches npm-style resolution and avoids
# broken module lookups in the main process without shamefully-hoist.
node-linker=hoisted
